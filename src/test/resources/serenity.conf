serenity {
  project.name = "OpenCart E2E Automation"
  test.root = "com.qa.opencart"
}

webdriver {
  driver = chrome
  autodownload = true
}

chrome {
  switches = "--start-maximized,--remote-allow-origins=*"
}
